# Single source of secrets for the Teclever Bid Portal backend.
# Copy this to $BIDPLUS_RUNTIME_DIR/.env (outside iCloud, gitignored) and fill it in.
# There is exactly ONE .env and ONE key for the whole system — do NOT create per-tool .env files.

ANTHROPIC_API_KEY=
