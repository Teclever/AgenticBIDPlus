# CLAUDE.md

**`AGENTS.md` drives this repo. Read it first and follow it — whether or not development is
being done with Claude.** It holds the operative rules, the build order (slices S0 → S6, each
gated by its DONE-WHEN), and pointers to the full design (`MASTER_ACTION_PLAN_V3.md`) and the
deploy runbook (`DEPLOY_WORKFLOW.md`).

This file intentionally adds nothing of its own — it exists only so Claude Code lands on
`AGENTS.md`. There is no Claude-specific behavior; the rules are the same for every tool.
