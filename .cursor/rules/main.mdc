---
description: Teclever Bid Portal — operative rules pointer (always on)
alwaysApply: true
---

`AGENTS.md` drives this repo. Read it first and follow it — it holds the operative rules, the
build order (slices S0 → S6, each gated by its DONE-WHEN), and pointers to the full design
(`MASTER_ACTION_PLAN_V3.md`) and the deploy runbook (`DEPLOY_WORKFLOW.md`).

This rule intentionally adds nothing of its own — it exists only so Cursor lands on
`AGENTS.md`. The rules are the same for every tool.
