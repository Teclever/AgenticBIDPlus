"""bidplus.web — the FastAPI read/act layer over parent.db (WEBAPP_DESIGN §16).

Deliberately empty so importing a submodule (``bidplus.web.schema`` /
``bidplus.web.passwords``) does NOT pull in FastAPI. The app object lives in
``bidplus.web.app:app``.
"""
